# ZKE 开发协作指南

本文件适用于 ZKE 仓库根目录及其所有子目录。除非更深层目录存在更具体的 `AGENTS.md`，否则所有在本仓库工作的 AI Agent
都必须遵守本文件。

## Canary

每次面向用户的回复都必须以独立一行 `ZKE 开发` 开头，下一行再写实际内容：

```text
ZKE 开发
<实际回复>
```

这是一个刻意保持简单的上下文标记。如果它停止出现，说明指令遵循可能正在衰减，例如发生了上下文污染或 context
rot。此时应对最近几次回复和改动保持怀疑，重新读取本文件、当前任务和相关文档；必要时开启新会话，只重新注入完成任务所需的最小上下文。

## 项目定位

ZKE（Z Kubernetes Engine）是构建在 Kubernetes 之上的 AI 原生 Kubernetes 管理与算力平台。

ZKE 当前处于早期设计与开发阶段：

- 部分模块尚未实现；
- 产品范围和技术选型仍可能调整；
- 当前版本不应被描述为生产可用；
- 不得虚构版本号、发布时间、用户数据、性能数据、认证结果或客户案例；
- 规划能力必须明确使用“计划”“规划”“拟支持”或“未来支持”等表述。

ZKE 不是 Linux 发行版，不是 Kubernetes 的替代品，也不应被描述为托管 Kubernetes 服务。

## 开始任务前

开始修改前，按任务需要读取以下内容：

1. `README.md`：项目定位、能力概览和主要入口；
2. `docs/README.md`：详细文档导航；
3. 与任务直接相关的 `docs/` 文档；
4. 目标代码、测试、配置与同目录约定；
5. 当前 Git 状态和已有未提交改动。

不要为了“了解项目”一次性读取所有文件。先定位任务作用域，再逐步加载必要上下文。

如果文档、代码和用户指令不一致：

1. 用户当前明确指令优先；
2. 已实现并经过测试的代码表示当前真实行为；
3. 文档表示产品意图和约束；
4. 无法安全判断时，说明冲突并请求确认，不要静默选择。

## 产品与架构不变量

除非任务明确要求更新设计，否则必须保持以下约束：

### Kubernetes 基础

- Kubernetes 是统一基础设施底座。
- 实际工作负载最终运行在具体 Kubernetes 集群中。
- 优先保持 Kubernetes 原生兼容性，避免不必要的平台锁定。
- 不宣称兼容所有 Kubernetes 发行版。

### Server + Agent

- ZKE 使用 Server + Agent 管理多个 Kubernetes 集群。
- 每个接入集群部署 ZKE Agent。
- Agent 主动连接 ZKE Server。
- 不要求 ZKE Server 直接访问 Kubernetes API Server。
- 查询和操作必须通过对应集群的 Agent 定域执行。
- 该模型需要适用于私有网络、混合云、多云和边缘集群。

### 资源作用域

- 遵循“全局观察，按集群执行”。
- 全局视图不代表全局操作权限。
- 跨集群查询必须遵守 Tenant、Project 和 RBAC 权限边界。
- 创建、变更和删除操作必须明确目标 Cluster，必要时还需明确 Namespace 和资源对象。
- 容器服务与终端按单集群应用设计。
- Agent 管理和可观测性平台按多集群应用设计。

## 开发工作流

### 1. 理解与定位

- 用搜索定位相关实现、接口、测试和文档。
- 先确认问题属于缺陷、功能、重构、文档还是基础设施变更。
- 识别受影响的 Server、Agent、Console、API、数据模型和权限边界。
- 对不稳定或尚未确定的设计，不要自行扩大实现范围。

### 2. 设计变更

- 优先做能够完整解决问题的最小变更。
- 沿用仓库已有结构、命名、错误处理和测试方式。
- 不为假设中的未来需求提前引入抽象。
- 新增跨模块行为时，明确数据流、失败路径、权限检查和审计边界。
- 新增外部依赖前，说明必要性并评估维护成本、安全性和许可证。

### 3. 实现

- 不修改与当前任务无关的文件。
- 不覆盖或回退用户已有改动。
- 不提交密钥、Token、kubeconfig、证书、真实集群地址或其他敏感信息。
- 示例配置只使用明显的占位值。
- 不虚构尚不存在的命令、API、Helm Chart、镜像地址或部署流程。
- 没有确定技术栈时，以仓库实际代码和配置为准，不提前锁定框架、语言或版本。

### 4. 验证

根据风险选择最低充分验证：

- 运行直接相关的单元测试；
- 运行受影响模块的类型检查、静态检查或构建；
- 对接口变更验证成功、失败、无权限和无效输入路径；
- 对集群操作验证目标 Cluster、Namespace 和资源身份不会丢失；
- 对敏感操作验证权限检查、用户确认和审计信息；
- 对文档变更检查相对链接、代码围栏与 Mermaid 语法。

如果因为环境、依赖或功能尚未实现而无法运行验证，必须明确说明未验证项和原因，不能写成“已通过”。

### 5. 交付

最终回复应简洁说明：

- 完成了什么；
- 关键文件或行为发生了什么变化；
- 执行了哪些验证；
- 是否存在未验证项、已知限制或需要用户决定的问题。

不要把工具调用过程、无关日志或大段实现细节堆入最终回复。

## 代码质量

- 使用清晰、稳定且符合领域语义的命名。
- 函数和模块保持单一职责，避免隐藏的跨集群副作用。
- 错误信息应包含可定位问题的上下文，但不得泄露敏感数据。
- 外部输入必须验证；权限判断不得只依赖前端。
- 网络调用需要考虑超时、取消、重试边界和幂等性。
- 重试不得导致重复创建、重复执行或重复审计。
- 并发逻辑必须明确资源所有权、生命周期和关闭路径。
- 日志应结构化，并在适用时携带 `tenant_id`、`project_id`、`cluster_id`、`namespace`、资源标识和请求关联 ID。
- 指标、日志和事件必须保留明确的集群身份，避免多集群数据串扰。

语言和框架相关格式以仓库配置为准。存在 formatter、linter、类型检查或测试工具时，优先使用项目已有命令，不另建平行规范。

## API 与兼容性

- API 必须显式表达租户、项目、集群和资源作用域。
- 破坏性变更应先更新设计或迁移方案，不能静默改变现有语义。
- 返回错误应区分认证失败、无权限、目标不存在、连接失败、执行失败和超时。
- Agent 与 Server 协议变更需要考虑版本差异和升级过程。

## 安全要求

- 默认最小权限和拒绝未授权访问。
- Secret、API Key、凭证和 Token 不得出现在日志、错误和遥测中。
- Web Terminal、Pod 日志、YAML 编辑和资源删除属于敏感能力。
- 敏感操作必须验证身份、权限、目标、操作内容和潜在影响。
- 审计日志应记录发起者、目标作用域、操作类型、结果和时间，并避免记录敏感正文。
- 不得声称项目已通过任何安全、云原生或 Kubernetes 认证，除非仓库中存在可验证证据。

## 文档约定

- `README.md` 只作为项目首页，保持简洁。
- 详细产品与设计文档存放在 `docs/`。
- 新增文档后更新 `docs/README.md` 导航。
- 架构说明存放在 `docs/architecture/`。
- 产品与交互说明存放在 `docs/product/`。
- 功能设计存放在 `docs/features/`。
- 安全和权限设计存放在 `docs/security/`。
- 完整开发规划维护在 `docs/roadmap.md`。
- 文档以简体中文为主，保留必要的英文技术名称。
- 只描述可验证的当前状态；规划内容必须明确标记。
- 不添加无法验证的 CI、Coverage、Release 或认证徽章。

代码行为、接口、配置、依赖或用户流程发生变化时，同步更新对应文档。不要把详细设计重新堆回根 README。

## Git 与变更纪律

- 开始和结束任务时检查 Git 状态。
- 将已有未提交改动视为用户工作，除非明确获准，不得回退、覆盖或清理。
- 不使用 `git reset --hard`、强制 checkout 或其他破坏性方式丢弃改动。
- 不主动提交、推送、创建分支或创建 Pull Request，除非用户明确要求。
- 一个变更应围绕一个清晰目的，避免混入无关格式化或批量重写。
- 不修改生成文件，除非仓库工作流明确要求并能重新生成验证。

## 完成标准

只有满足以下条件时，任务才算完成：

- 请求的行为或文档已经实际落地；
- 产品与架构不变量没有被意外破坏；
- 权限、目标作用域、失败路径和敏感信息得到适当处理；
- 已执行与变更风险相匹配的验证；
- 相关文档和导航已经同步；
- 没有覆盖用户的无关改动；
- 最终回复明确报告结果和限制。
